# tarea1_IA

 Librerias a instalar:

pip install --upgrade pip

pip install pgmpy hmmlearn pandas numpy matplotlib scikit-learn jupyter kagglehub
  
Si hmmlearn falla al instalar (necesita compilar C), correr primero:

sudo dnf install gcc gcc-c++ python3-devel -y

y reintentar  el pip install 